/*
import React, {FunctionComponent} from "react";

const buttonVariant = cva()

export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<ButtonProps> {
    icon?: FunctionComponent<any>
}

export default function Button(props: ButtonProps) {
    return(<button className={buttonVariant({size, variant})}>
        {children}
    </button>);
}

*/
